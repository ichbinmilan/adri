{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('projects') }}">
        <div class="big-pic" style="background-image: url('{{ asset('projects.jpg') }}')">
            <h2 style="position: absolute; bottom: 50px; right: 8%">Projects</h2>
        </div>
    </a>
    <a href="{{ path('about') }}">
        <div class="my-5 big-pic" style="background-image: url('{{ asset('about.jpg') }}')">
            <h2 style="position: absolute; bottom: 50%; left: 8%">About</h2>
        </div>
    </a>
    <a href="{{ path('contacts') }}">
        <div class="my-5 big-pic" style="background-image: url('{{ asset('contacts.jpg') }}')">
            <h2 style="position: absolute; top: 50px; right: 8%">Contacts</h2>
        </div>
    </a>
{% endblock %}

{% block menu %}{% endblock %}
